import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  container: {
    width: "100%",
    padding: "1rem 6%",
    textAlign: "center",
  },
  formBox: {
    padding: "2rem",
    margin: "2rem auto",
    borderRadius: "1.5rem",
  },
}));
